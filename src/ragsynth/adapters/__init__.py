"""Offline-first adapters behind small Protocols (SPEC §12).

Concretes live here and in the composition root only (DIP, SPEC §3.2);
steps and metrics depend on the Protocols in each subpackage's ``base``.
"""
