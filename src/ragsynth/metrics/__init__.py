"""Evaluation metric suites: fidelity, efficiency, diversity, validity.

Import from submodules directly (``ragsynth.metrics.fidelity`` etc.);
this package intentionally re-exports nothing so the four suites stay
independently importable.
"""
