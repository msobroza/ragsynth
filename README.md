# ragsynth

Synthetic query generation & validation toolkit for RAG retrieval evaluation
over dynamic knowledge bases. See `SPEC.md` (source of truth) and `PLAN.md`.

Quickstart (populated in Phase 4):

```bash
uv sync
uv run ragsynth run --config configs/v1_toy.yaml
```
